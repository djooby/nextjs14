import "../globals.css";
export const metadata = {
  title: "Dashboard - NextJS 14",
  description: "Dashboard - NextJS 14",
};

export default function RootLayout({ children }) {
  return children;
}
